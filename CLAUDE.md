# CLAUDE.md（Pokemon-go-tools）

このファイルは Claude Code がこのリポジトリで作業する際に必ず参照する指針です。
各アプリの詳細仕様は `spec/` 配下の対応する仕様書を参照してください。

---

# 〔仕様書を正とする：最優先で守る〕

- 仕様書（`spec/*_spec.md`）に書いてあることを正とする。もっと良い方法があれば、勝手に直さず提案すること。
- フォルダ構成の正は `spec/repository_overview.md`。決定が構成に波及したら、この文書へ反映漏れがないか照合する。
- 仕様書・CLAUDE.md に明記がない判断が必要になったら、勝手に進めず確認を求める（選択肢と推奨案を示す）。

---

# 〔プロジェクト固有〕

## 参照対象外フォルダ・ファイル

以下は、ユーザーから明示的な指示がない限り、**読み込まない・参照しない・編集しない**（調査・要約・検索でも除外する）。

- `code/`・`output/`：作業用メモ。Git 管理外。
- `shared/`・`scp_checker/`・`iv_strings_generator/`・`tools/`：パイロット中は触らない既存資産（後続の移行で扱う）。
  - 例外：現行 `id_generator/app.py` の挙動を理解するために読むのは可。ただし `shared/` 自体は編集しない。パイロットのデータは `master_data/`（`shared/` からの一時コピー）を読む。コピーの作成はユーザーが行う。
- `.venv/`：仮想環境。
- `ClaudeCode対象外/`：人間用の引き継ぎ資料など。**読み込まない・参照しない・編集しない**（調査・要約・検索でも必ず除外する）。

## プロジェクト概要

ポケモンGO の PvP 支援ツール群。Streamlit Community Cloud で Web アプリとして公開している。
現在は **id_generator を新フォルダ構成（種類で割る X 構成）へ移行するパイロット**の段階。まず id_generator だけを対象とし、成功後に他アプリへ横展開する。

## 技術スタック

| 項目 | 値 |
|------|-----|
| 言語 | Python 3.12 |
| UI | Streamlit |
| 品質 | pytest / mypy / black / ruff |

- **外部サービス・API・DB・機密情報はいずれも無い**（純粋計算のアプリ）。
- Python バージョンは実装の前提としてここに明記する（不一致は型チェック・テストをすり抜けうるため）。

## ディレクトリ構成

構成図は CLAUDE.md に持たない。`spec/repository_overview.md` を正として参照する。4層の原則は下記「アーキテクチャ」を参照。

## アーキテクチャ

コードを層に分ける。上位が下位を呼び、下位は上位を知らない。features は library と esal のどちらも直接呼べる。library は esal を呼んでよい。逆向き（esal→features 等）は禁止。

1. **features（機能層 / `src/<app>/`）**：各アプリ固有のロジック。公開関数を1つ定義し、そのシグネチャは呼び出し側が依存するため固定する。内部の補助は `_` プレフィックス。
2. **library（共通処理層 / `src/library/`）**：2か所以上のアプリから使われる純粋な共通部品（SCP計算・ファミリー展開・共有 dataclass 等）。**予想で作らず、重複が実際に現れてから作る。**
3. **esal（ファイル読み込み層 / `src/esal/`）**：**このプロジェクトでは「外部サービス」ではなくデータファイルの読み込みがesalにあたる。** `master_data/` 等の読み取りをここに集約する。読み込むファイルのパスはモジュール定数として持つ。Streamlit 依存（`@st.cache_data` 等）をここに持ち込まない。
4. **エントリポイント（`<app>/app.py`）**：Streamlit の薄い UI 層。features を呼ぶだけ。例外の捕捉と画面表示はここに集約する。Streamlit のキャッシュが必要ならこの層で付ける。**この層は black / ruff（整形・リント）の対象だが、mypy / pytest（型・テスト）の対象外**（src/ の外にある薄い UI のため）。

## 機密・セキュリティ要件

- 該当なし。外部送信も API キーも機密情報も扱わない。将来これらを導入する場合は仕様書とこの節を更新してから実装する。

## 学習用コメントモード：ON

- Python 特有の書き方（内包表記・with・デコレータ等）に行内コメントで補足し、複雑なロジックには「なぜそうしているか」の日本語コメントを通常より多めに付ける。
- どのモードでも `i += 1  # iを1増やす` のような無意味なコメントは禁止。

---

# 〔汎用：原則このまま使う〕

## コーディング規約

- シンプルで読みやすいことを最優先。意味のある名前を付ける（`data`・`tmp` を避ける）。マジックナンバーは定数化。型ヒント必須。本番コードに `print` を残さない。
- **外部仕様で決まりユーザーが触らない値（ファイルパス・フォーマット定義等）は、該当モジュール冒頭に `_` 付きモジュール定数として宣言する**（ロジック内に直書きしない）。
- **文字エンコーディング**：`subprocess` や `open()` でテキストを読み書きするときは必ず `encoding="utf-8"` を明示する。Windows の Python は既定が cp932 のため、明示しないと日本語で `UnicodeDecodeError` が出る。本番コード・スクリプト・テストすべてに適用する。緩めるのではなく、エンコーディングを正すこと。

## エラーハンドリング方針

- モジュールはエラー時に**例外を raise する**（握りつぶさない）。
- 例外の捕捉と表示は呼び出し元（UI 層・エントリポイント）に集約する。

## テストの書き方

### 対象と配置
- テストは `tests/` 配下に、`src/` と同じフォルダ階層・同じ名前で配置する（例：`src/id_generator/` → `tests/id_generator/test_id_generator.py`）。
- テストは機能単位で、その機能の公開関数に対して書く（実装ファイルとの1対1対応は求めない）。
- 各テストフォルダに空の `__init__.py` を置く。
- 対象は純粋ロジック。UI・ファイル読み込みの直接操作はモックで遮断する。対象外にした範囲は報告する。

### 網羅の基準
- 実装内の**各 raise には、それを発生させるテストを必ず1つ以上**書く（これを実質的な網羅基準とする）。
- カバレッジ 100% は努力目標であり、機械的な合否条件にはしない。
- **テストを通すために実装側を変更（防御コードの削除・分岐の簡略化）しない。**

### テストの質（禁止事項）
- モックの戻り値をそのまま assert するだけのテスト。
- テスト対象と同じ計算をテスト側に複製して比較するテスト。
- 出力の正しさを検証しないテスト（「例外が出ない」「None でない」だけで終わらせない。値・内容まで検証する）。

### モック（外部依存の差し替え）
- feature のテストは、呼び先の esal（ファイル読み込み）を `unittest.mock` の `patch` でスタブ化し、本物を呼ばせない。`pytest-mock` は使わない。
- **`patch` 対象は「使用箇所のパス」を指定する**（定義元ではなく、テスト対象が import して使っている名前）。定義元を指定すると差し替えが効かず、本物が呼ばれてテストが静かに誤動作する。
- `patch` には `autospec=True` を付ける。
- モックの呼び出し（回数・引数）を検証する（正常系は `assert_called_once_with(...)`）。例外の模擬は `side_effect` を使い、伝播を検証する。

## 品質チェック（フックで自動実行される）

- ファイル編集のたびに black / ruff が、作業終了時に mypy / pytest がフック（`.claude/settings.json`）により自動実行される。
- フックの失敗は必ず解消してから完了すること。フックを無効化・回避しない。
- **失敗を解消する際、テスト側を弱める変更（assert の緩和・skip 化・テスト削除）をしてはならない。** テストが誤っていると判断した場合は、変更せず理由を報告して確認を求めること。

## requirements.txt の運用

- 直接依存（自分が選んだライブラリ）のみを `<名前>==<バージョン>` で固定する。間接依存は pip の解決に任せる。
- ライブラリの追加・更新はユーザーの承認を得てから行う。

## Git・禁止事項

- コミット・push はユーザーが行う。Claude Code は `git push` を実行しない。完了時にステージ対象とコミットメッセージ案を提示する。
- ❌ `except: pass` 等で例外を握り潰す。
- ❌ `print` を本番コードに残す。
- ❌ カバレッジや品質チェックを通すことを目的に、実装・テストを歪める。
- ❌ ユーザーの承認なくライブラリを追加・更新する。
- ❌ 参照対象外フォルダ（`code/`・`output/`・既存アプリ・`shared/` 等）を指示なく読み込む・編集する。

---

## 改訂履歴

| Rev | 日付 | 内容 |
|-----|------|------|
| 0.1 | 2026-07-18 | 初版。BoxNavi の CLAUDE.md を土台に、ガードレール中心・最小構成で作成。esal＝ファイル読み込み層と読み替え、仕様書のみ運用、参照対象外に code/・output/・既存アプリを指定。パイロット＝id_generator。 |
